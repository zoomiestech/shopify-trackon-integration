services:
  - type: web
    name: shopify-trackon-bridge
    runtime: docker
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    disk:
      name: trackon-data
      mountPath: /app/data
      sizeGB: 1
